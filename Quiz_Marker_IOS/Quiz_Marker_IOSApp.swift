import SwiftUI

@main
struct QuizMarkerApp: App {
    var body: some Scene {
        WindowGroup {
            // This is the "root" of your app, like main_window in Tkinter
            MainMenuView()
        }
    }
}
